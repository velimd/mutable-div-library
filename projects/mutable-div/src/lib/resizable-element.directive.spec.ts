import { ResizableElementDirective } from './resizable-element.directive';

describe('ResizableElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableElementDirective();
    expect(directive).toBeTruthy();
  });
});
