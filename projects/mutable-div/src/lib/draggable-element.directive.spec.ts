import { DraggableElementDirective } from './draggable-element.directive';

describe('DraggableElementDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableElementDirective();
    expect(directive).toBeTruthy();
  });
});
